# LyricRelay 项目协作约定

## 项目边界

- MVP 只做 Android → Windows 的同步歌词，不传输手机音频。
- 不加入账号、云同步、播客、ASR、桌面悬浮歌词或多设备连接。
- 任何新功能先确认它是否直接服务于“手机听，电脑看”。

## 模块边界

- `apps/android-companion` 只负责读取 MediaSession、维护手机侧连接和发送状态。
- `apps/windows-client` 只负责连接、歌词获取、时间轴计算、任务栏渲染和本地设置。
- `packages/protocol` 是跨端唯一消息契约；业务模块不要复制一份私有协议。
- `docs` 记录稳定的架构和开发约定；实现发生变化时同步更新文档。

## 实现约束

- 时间轴计算使用单调时钟，不使用设备墙上时间推算播放进度。
- Android 不以高频进度消息驱动同步；状态变化立即发送，播放中按校准周期发送。
- Provider、渲染器和设备链路必须通过接口隔离，便于替换实现和测试。
- 日志不得记录配对密钥、完整二维码内容或不必要的用户数据。
- 先写能验证行为的测试，再做跨平台适配和 UI 细节。

## 变更检查

提交前至少确认：

1. 没有扩大 MVP 范围。
2. 协议变化已更新 `packages/protocol` 和相关测试说明。
3. 失败重试、暂停、Seek、切歌等状态变化仍有明确行为。
4. 文档中的路径、命令和模块名称与仓库一致。

